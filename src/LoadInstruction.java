public class LoadInstruction {
/*
    
    // LD r1, r2 (ex 0x7f)
    public int LoadRegisterIntoRegister(Register r1, Register r2) {
        r1.write(r2.get());
        return 4;
    }

    // LD A, (HL) (ex 0x7e)
    public int LoadMemoryAddressedByRegisterIntoRegister(Register r1, Register r2, Register r3) {
        r1.write(memory.get(Registers.get16BitRegister(r2, r3)));
        return 8;
    }

    // LD (HL), B (ex 0x70)
    public int LoadRegisterIntoMemoryAddressedByRegister(Register r1, Register r2, Register r3) {
        Registers.write16BitRegister(r1, r2, r3.get());
        return 8;
    }

    // LD (HL), n (ex 0x36)
    public int LoadImmediateValueIntoMemoryAddressedByRegister(Register r1, Register r2, int value) {
        Registers.write16BitRegister(r1, r2, value);
        return 12;
    }

    // LD A, (nn) (ex 0xfa)
    public int LoadMemoryAddressedByImmediateValueIntoRegister(Register r1, int value) {
        r1.write(memory.get(value));
        return 16;
    }

    // LD B, n (ex: 0x06) 
    public int LoadValueIntoRegister(int value, Register r1) {
        r1.write(value);
        return 8;
    }

    // LD r1r2, nn (ex: 0x01)
    public int LoadValueInto16BitRegister(int value, Register r1, Register r2){
        Registers.write16BitRegister(r1, r2, value);
        return 12;
    }

    public int

    */
}
